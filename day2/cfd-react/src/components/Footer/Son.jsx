import './style.scss'

const Son = ({tag}) =>{
    return(
        <div className="">
            {/* {tags.map( (tagItem) => <h2 key={tagItem}>{tagItem}</h2>)} */}
            <h2>{tag}</h2>
        </div>
    )
}
export default Son